{
  "name": "scalp-care-monorepo",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "pnpm -r --filter ./apps/* dev",
    "build": "pnpm -r --filter ./apps/* build",
    "start": "pnpm -r --filter ./apps/* start",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "@types/node": "^20.10.5",
    "eslint": "^8.56.0",
    "eslint-config-next": "^14.0.4",
    "prettier": "^3.1.1",
    "typescript": "^5.3.3"
  }
}
