# Codex Prompt: sake-haccp-bar-ui v1.0
# 日本酒バー専用 HACCP 管理 UI モック

あなたは「GPT-5.1-Codex-Max」として動作します。
以下の仕様にもとづき、**日本酒バー専用の HACCP 管理 Web UI モック** を作成してください。

出力は **仕様理解 → 簡易設計 → コード生成** の3フェーズで行ってください。

================================================
1. 目的（Purpose）
================================================

小規模な日本酒バー向けに、HACCP に基づく以下の管理を
ブラウザ上で簡単に記録・確認できる **Web UI モック** を作成します。

- 日本酒用冷蔵庫などの温度記録
- 日本酒の開栓日・保存状態の管理
- 日次／週次の清掃チェック
- 従業員衛生チェック（出勤時）
- 廃棄記録（酒／食材）
- 今日の完了状況のダッシュボード表示

あくまで **モック（スタンドアロン）** とし、
**HTML + CSS + JavaScript（プレーン）だけで完結** させてください。
バックエンドやDB接続は不要です。

================================================
2. スコープ（Scope）
================================================

生成するファイルは以下の3つです。

- index.html
- style.css
- app.js

画面（ビュー）は以下とします。

- H_TOP   : 今日のHACCP状況ダッシュボード
- H_TEMP  : 温度記録画面（日本酒用冷蔵庫など）
- H_SAKE  : 日本酒ボトル管理（開栓日・保存状態）
- H_CLEAN : 清掃チェック（日次／週次）
- H_STAFF : 従業員衛生チェック（出勤時）
- H_LOG   : 記録ログ一覧（温度／清掃／廃棄など）

SPA 風構成とし、画面遷移は **タブ or メニュー切替による表示切替** で実装してください。

================================================
3. 機能仕様（Functional Requirements）
================================================

--------------------------------
3-1. H_TOP：HACCP ダッシュボード
--------------------------------

要素：

- 今日の日付表示
- カテゴリ別進捗（バー表示 or パーセンテージ）
  - 温度管理      ： 完了数 / 必要件数
  - 日本酒管理    ： 今日確認済みの本数
  - 清掃（日次）  ： 完了チェック数 / 項目数
  - 従業員衛生    ： 今日チェック済み人数
- 最近のログ（最新3件）
  - 例：「冷蔵庫A 5.2℃ 記録」「カウンター清掃 完了」など

--------------------------------
3-2. H_TEMP：温度記録画面
--------------------------------

対象例：

- 日本酒用冷蔵庫A
- 日本酒用冷蔵庫B
- 冷凍庫（ある場合）
- ショーケース

UI仕様：

- 一覧形式で各設備を表示：
  - 「設備名」「入力欄（温度）」「[記録]ボタン」
- [記録]押下時：
  - 入力温度・現在日時・設備名を `temperatureLogs[]` に保存
  - 温度が基準外の場合は「異常」フラグを立てる（例：0〜15℃以外を異常としてよい）

--------------------------------
3-3. H_SAKE：日本酒ボトル管理
--------------------------------

テストデータとして、最低5本の日本酒を登録してください。

各ボトルには以下の情報を持たせてください。

- `id`              : 連番
- `name`            : 酒名
- `type`            : 純米 / 吟醸 / 生酒 など
- `storage`         : 保存場所（冷蔵庫Aなど）
- `openedDate`      : 開栓日（文字列）
- `recommendedDays` : 開栓後の推奨提供期間（日数、例：7）
- `status`          : 「良好」「そろそろ飲み切りたい」「廃棄検討」など

H_SAKE画面では：

- ボトル一覧（テーブル or カード）
- 「開栓から何日経過したか」を app.js 内で計算し、
  - 推奨日数を超えたものは強調表示（背景色 or ラベル）
- 任意で「状態」を更新する簡易UI（プルダウンやボタン）を用意しても良い

--------------------------------
3-4. H_CLEAN：清掃チェック
--------------------------------

日次清掃項目の例（最低5項目）：

- カウンター清掃
- シンク・作業台清掃
- ゴミ処理・分別確認
- グラス棚の拭き上げ
- トイレ清掃
- 床清掃

UI仕様：

- チェックボックスリスト＋「[保存]」ボタン
- 保存時に `cleanLogs[]` に「日付・時刻・項目・スタッフ・完了フラグ」をpush
- 日次進捗は H_TOP に反映できるようにする

--------------------------------
3-5. H_STAFF：従業員衛生チェック
--------------------------------

想定：

- 出勤時に「手洗い」「服装」「体調」などを確認して記録

UI仕様：

- スタッフ名（テキスト or セレクト）
- チェック項目（最低3つ）：
  - 手洗い／爪／傷
  - 服装（清潔）
  - 体調（問題なし／要注意）
- [記録] ボタンで `staffLogs[]` に保存

--------------------------------
3-6. H_LOG：記録ログ一覧
--------------------------------

- 温度記録・清掃記録・スタッフ衛生記録・日本酒状態更新などをまとめて時系列で表示
- 表示項目例：
  - 日付・時刻
  - カテゴリ（温度 / 清掃 / スタッフ / 酒）
  - 内容（例：「冷蔵庫A 5.2℃ 記録」「酒名○○ 開栓後10日 経過」など）

================================================
4. データモデル（Data Model）
================================================

app.js 内に、以下のような構造のモックデータを定義してください。

### 日本酒ボトルデータ（例）

```js
const sakeList = [
  {
    id: 1,
    name: "○○ 純米酒",
    type: "純米酒",
    storage: "冷蔵庫A",
    openedDate: "2025-12-01",
    recommendedDays: 7,
    status: "良好",
    note: "冷酒〜ぬる燗向き。早めに出したい1本。"
  },
  // ...最低5件
];
```

### 温度ログ

```js
const temperatureLogs = [
  // { id, device, value, timestamp, isAlert }
];
```

### 清掃ログ

```js
const cleanLogs = [
  // { id, task, done, staff, timestamp }
];
```

### 従業員衛生ログ

```js
const staffLogs = [
  // { id, staff, condition, notes, timestamp }
];
```

### 共通ログ（H_LOG用）

任意ですが、複数ログをまとめて表示するための
activityLogs[] のような配列を用意しても構いません。

================================================
5. 非機能要件・制約（Non Functional）
•HTML / CSS / JavaScript（プレーン）のみ使用すること
•フレームワーク（React/Vue 等）は使用しないこと
•画像ファイルやバイナリは使わず、色や枠線で状態を表現すること
•日本語ラベルでUIを作成すること
•index.html をブラウザで開くだけで動作すること

CSSテーマの方針：
•ベースはシンプル
•日本酒バーっぽく「やや暗めの背景＋アクセントに暖色」を使う
•カードやセクションに余白を取り、情報を詰め込みすぎない

================================================
6. コーディング方針（Coding Style）
•JavaScriptはES6（const / let / アロー関数）を使用
•関数は役割ごとに分割し、コメントをつけること
•例：renderDashboard(), renderTempPage(), renderSakePage(), addTemperatureLog() など
•DOM操作は document.getElementById / querySelector で行う

================================================
7. 進め方（フェーズ）— Specification Driven

あなたは以下の順番で出力してください。

フェーズ1：仕様の要約
•上記仕様を読み、あなたが理解した内容を5〜10行の箇条書きで要約してください。
•必要な画面、データ構造、主なイベント（クリックなど）を列挙してください。
•このフェーズでは まだコードを書かない こと。

フェーズ2：簡易設計（テキスト）
•HTML の大まかな構造（header / nav / main / 各画面のラッパーdiv など）をテキストで説明してください。
•JavaScript の主要関数名と、その役割をリストアップしてください。
•CSS で表現する主なコンポーネント（カード、ボタン、進捗バーなど）の概要を述べてください。

フェーズ3：実装（コード出力）
•最後に、以下の順番でコードをすべて出力してください。

1.=== index.html === の見出しのあとに index.html の完全なコード
2.=== style.css === の見出しのあとに style.css の完全なコード
3.=== app.js === の見出しのあとに app.js の完全なコード

以上の仕様と進め方に従って、フェーズ1から順番に出力してください。

---

もし「もっと画面を絞りたい」「スタッフ衛生は別仕様にしたい」などあれば、  
このプロンプトの該当部分だけ削って使えばOKです。

次にやるとしたら：

- 生成されたコードに合わせた **UI改良案**  
- このHACCP UIと、さっきの「肩代わりUIコンセプト」とを結んだ **提案スライド**  

なんかも一緒に作れますよ。
