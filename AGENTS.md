# AGENTS.md

## プロジェクト目的
`otona.to.w` のWebサイト / LP制作に向けて、実装前のハーネス設計文書を整備し、ブランド解釈・世界観・運用方針・将来拡張を一貫させる。

## 作業ルール
- 実装より先に設計文書を読む。
- `docs/README.md` を起点に読み進める。
- 推論フェーズ（設計）と実装フェーズ（UI/コード）を分離する。

## ブランド解釈の基本
- `otona.to.w` は「構造記号・接続・遷移」を重視して扱う。
- `.to.` は日常から夜、個から対話、酒から空間への移行を示す。
- `w` は固定語に閉じず、weave / warmth / widen / whisper / wood / wine / whisky / window / with などの余白を持つ。

## 表現ガードレール
- 成人向けだが露骨なアダルト方向にはしない。
- 静かなバー / ラウンジ / 酒場的世界観を維持する。
- 過度な高級感、過度な大衆感、過度な性的表現を避ける。
- AIっぽい均質デザインを避け、手触りのある差異と余白を残す。

## 運用と更新
- メニュー更新・季節更新を前提に設計する。
- 店舗運用者が手で更新できる余地を確保する。
- 画像素材は後続フェーズで生成・精製する（初期は方針とプロンプト定義まで）。

## 成長方針
- 一度作って終わりではなく、小さく育つLP / Webサイトとして設計する。
