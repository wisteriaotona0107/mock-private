# AGENTS.md

## プロジェクト作業ポリシー
- いきなり実装を開始せず、必ずフェーズ単位で作業する（設計→検証→実装）。
- 仕様変更が発生した場合は、コードより先に `docs/` の関連文書へ反映する。
- ゲームロジック（判定・スコア・進行）と見た目（UI・演出・素材）を分離して設計・実装する。
- 画像・キャラクター・背景・UI装飾などの素材は、後工程でGPT Image系生成を前提とし、現段階ではプレースホルダ運用にする。
- 表現は「バカらしいが少し怖い」トーンを維持しつつ、差別・身体的特徴への嘲笑・実在人物攻撃につながる内容を避ける。
- 変更後は簡単な自己レビュー（不足・矛盾・過剰実装の有無）を行い、必要なら文書へ追記する。
- Markdown文書を重視し、後続フェーズの実装担当が読んで着手しやすい構成にする。

## 現フェーズの前提
- 現在はハーネス設計フェーズであり、実装コードの追加は行わない。
- まず設計文書を整え、受け入れ条件を満たしてから次フェーズへ進む。
